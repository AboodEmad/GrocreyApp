import 'dart:ui';

class NotificationModel {
  String title, subTitle, time, icon;
  Color color;

  NotificationModel({
    required this.title,
    required this.subTitle,
    required this.time,
    required this.icon,
    required this.color,
  });
}
